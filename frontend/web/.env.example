# Leave unset to use same-origin /api (recommended for local dev + Vercel).
#
# If you set this, it MUST end with /api (Express mounts routes under /api).
# Wrong: http://localhost:4000
# Right: http://localhost:4000/api
#
# VITE_API_BASE_URL=http://localhost:4000/api
